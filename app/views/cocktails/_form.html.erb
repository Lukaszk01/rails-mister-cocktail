<%= simple_form_for(@cocktail) do |f| %>
<%= f.input :name %>
<%= f.input :photo, as: :file %>
<%= f.button :submit  %>
<div class="form-actions btn btn-primary">
<%= f.button :submit %>
  </div>
<% end %>

